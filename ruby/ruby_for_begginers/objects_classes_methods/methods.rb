# Every method always returns exactly one object. 
# If we don't do anything else, then a method will return value of the last evaluated statement.
#

def add_two(number)
  number + 2
end

puts add_two(2)

puts number
